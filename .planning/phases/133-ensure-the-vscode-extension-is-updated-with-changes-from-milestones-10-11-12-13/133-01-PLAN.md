---
phase: 133-ensure-the-vscode-extension-is-updated-with-changes-from-milestones-10-11-12-13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json
  - compiler/mesh-lsp/src/completion.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "The `json` keyword is highlighted as a declaration keyword in .mpl files"
    - "Regex literals like `~r/pattern/flags` are highlighted as strings in .mpl files"
    - "Slot pipe operators like `|2>` and `|3>` are highlighted as pipe operators"
    - "Atom literals like `:asc`, `:email` are highlighted as constants"
    - "The `json` keyword appears in LSP completion suggestions"
    - "Snippets for `type` alias and `json {}` block appear in LSP completions"
    - "All 49 Mesh keywords are in the LSP KEYWORDS list"
  artifacts:
    - path: "tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json"
      provides: "TextMate grammar with all m10-m13 syntax forms"
      contains: "json"
    - path: "compiler/mesh-lsp/src/completion.rs"
      provides: "LSP completion with all 49 keywords and new snippets"
      contains: "\"json\""
  key_links:
    - from: "tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json"
      to: "keyword.declaration.mesh"
      via: "match pattern"
      pattern: "json"
    - from: "compiler/mesh-lsp/src/completion.rs"
      to: "KEYWORDS constant"
      via: "array entry"
      pattern: "\"json\""
---

<objective>
Update the TextMate grammar and LSP completion list to reflect all language additions from milestones 10-13.

Purpose: The extension was last updated at v0.2.0 (2026-02-14) before milestones 11-13 shipped. New syntax forms — `json {}` literals, `~r//` regex literals, `|N>` slot pipe operators, atom literals, and `nil` — are currently unhighlighted or misclassified. The LSP completion list is also missing `json` from its 49-keyword vocabulary.

Output: Updated grammar file + updated LSP completion source.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current grammar keyword groups from tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json -->
<!-- Executor should update these patterns directly — no exploration needed. -->

Current keyword.control pattern:
```
"\\b(if|else|case|match|when|do|end|return|from|import|for|while|cond|break|continue)\\b"
```

Current keyword.declaration pattern:
```
"\\b(fn|let|def|type|struct|module|interface|impl|pub|actor|service|supervisor|call|cast|trait|alias)\\b"
```

Current keyword.operator pattern:
```
"\\b(and|or|not|in|where|with|as|spawn|send|receive|self|link|monitor|terminate|trap|after)\\b"
```

Current constant.language pattern:
```
"\\b(true|false)\\b"
```

Current support.type pattern:
```
"\\b(Int|Float|String|Bool|Unit|Pid|Option|Result|List|Map|Set|Queue|Json)\\b"
```

Current support.function pattern:
```
"\\b(Some|None|Ok|Err)\\b"
```

Current pipe operator pattern:
```
"\\|>"
```

<!-- From compiler/mesh-common/src/token.rs — all 49 keywords as of m13: -->
<!-- actor after alias and break call case cast cond continue def do else end false fn for if impl import in interface json let link match module monitor nil not or pub receive return self send service spawn struct supervisor terminate trait trap true type when where while with -->

<!-- From compiler/mesh-lsp/src/completion.rs — current KEYWORDS list (48 entries, MISSING "json"): -->
const KEYWORDS: &[&str] = &[
    "actor", "after", "alias", "and", "break", "call", "case", "cast", "cond", "continue",
    "def", "do", "else", "end", "false", "fn", "for", "if", "impl", "import", "in",
    "interface", "let", "link", "match", "module", "monitor", "nil", "not", "or", "pub",
    "receive", "return", "self", "send", "service", "spawn", "struct", "supervisor",
    "terminate", "trait", "trap", "true", "type", "when", "where", "while", "with",
];

<!-- From compiler/mesh-lsp/src/completion.rs — current SNIPPETS (9 entries): -->
("fn", ...), ("let", ...), ("struct", ...), ("case", ...), ("for", ...), ("while", ...), ("actor", ...), ("interface", ...), ("impl", ...)

<!-- From compiler/mesh-lsp/src/completion.rs — current comment says "48 keywords + 12 types + 9 snippets = 69 minimum" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TextMate grammar for all m10-m13 syntax additions</name>
  <files>tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json</files>
  <action>
Update the grammar JSON file with these specific changes:

1. **Add `json` to keyword.declaration pattern** — `json` is a declaration keyword (like `struct`, `type`) used before `{`:
   Change: `"\\b(fn|let|def|type|struct|module|interface|impl|pub|actor|service|supervisor|call|cast|trait|alias)\\b"`
   To: `"\\b(fn|let|def|type|struct|module|interface|impl|pub|actor|service|supervisor|call|cast|trait|alias|json)\\b"`

2. **Add `nil` to constant.language pattern** — `nil` is a constant value like `true`/`false`:
   Change: `"\\b(true|false)\\b"`
   To: `"\\b(true|false|nil)\\b"`

3. **Add atom literal highlighting** — atoms are `:identifier` patterns (`:asc`, `:email`, `:name`). Add a new pattern entry in the `"repository"` section BEFORE the `"keywords"` entry, and include it in the top-level `"patterns"` array BEFORE `"#keywords"`:

   New repository entry:
   ```json
   "atoms": {
     "patterns": [
       {
         "name": "constant.language.atom.mesh",
         "match": ":[a-zA-Z_][a-zA-Z0-9_]*"
       }
     ]
   }
   ```

   Add `{ "include": "#atoms" }` to the top-level patterns array, placed BEFORE `{ "include": "#strings" }` to take priority.

4. **Add regex literal highlighting** — regex is `~r/pattern/flags`. Add a new repository entry for `"regex-literals"`:

   ```json
   "regex-literals": {
     "patterns": [
       {
         "name": "string.regexp.mesh",
         "match": "~r/(?:[^/\\\\]|\\\\.)*(?:/[ims]*)?"
       }
     ]
   }
   ```

   Add `{ "include": "#regex-literals" }` to the top-level patterns array, placed BEFORE `{ "include": "#strings" }`.

5. **Update pipe operator pattern to capture slot pipe `|N>`** — slot pipe is `|2>`, `|3>` etc. The current pattern `\\|>` only matches `|>`. Update the pipe operator pattern to also match `|[0-9]+>`:

   Change the pipe operator entry from:
   ```json
   {
     "name": "keyword.operator.pipe.mesh",
     "match": "\\|>"
   }
   ```
   To:
   ```json
   {
     "name": "keyword.operator.pipe.mesh",
     "match": "\\|[0-9]*>"
   }
   ```
   This matches `|>` (zero digits) and `|2>`, `|3>` etc. (one or more digits) with the same scope.

The file is JSON — keep all other content exactly as-is. Validate that the result is valid JSON before finishing (use `python3 -c "import json; json.load(open('tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json'))"` from the repo root).
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/snow && python3 -c "import json; data=json.load(open('tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json')); kw=next(p for p in data['repository']['keywords']['patterns'] if 'keyword.declaration' in p.get('name','')); assert 'json' in kw['match'], 'json missing from declaration keywords'; const=next(p for p in data['repository']['keywords']['patterns'] if 'constant.language' in p.get('name','')); assert 'nil' in const['match'], 'nil missing from constants'; assert 'atoms' in data['repository'], 'atoms pattern missing'; assert 'regex-literals' in data['repository'], 'regex-literals pattern missing'; print('Grammar validation passed')"</automated>
  </verify>
  <done>
    - `json` appears in the keyword.declaration pattern
    - `nil` appears in the constant.language pattern
    - A new `atoms` repository rule exists matching `:[a-zA-Z_][a-zA-Z0-9_]*` with scope `constant.language.atom.mesh`
    - A new `regex-literals` repository rule exists matching `~r/...` with scope `string.regexp.mesh`
    - Pipe operator pattern updated to `\\|[0-9]*>` covering both `|>` and `|N>`
    - Grammar file is valid JSON
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `json` to LSP keyword list and add new snippets</name>
  <files>compiler/mesh-lsp/src/completion.rs</files>
  <action>
Make three targeted edits to `compiler/mesh-lsp/src/completion.rs`:

1. **Add `"json"` to the KEYWORDS constant** — insert it alphabetically between `"interface"` and `"in"`. The correct alphabetical position for `"json"` is after `"in"` and `"interface"`, before `"let"`. Current order around that spot: `"in", "interface", "let"`. Insert `"json"` between `"interface"` and `"let"`.

2. **Update the comment at the top** — change `"All 48 Mesh keywords"` to `"All 49 Mesh keywords"`.

3. **Add new snippets** to the SNIPPETS constant — insert these two entries after the existing `("impl", ...)` snippet:

   ```rust
   (
       "type",
       "type ${1:Alias} = ${0:ExistingType}",
   ),
   (
       "json",
       "json {\n  ${1:key}: ${0:value}\n}",
   ),
   ```

4. **Update the test assertion** — find the assertion `"expected at least 69 completions for empty prefix, got {}"` and update the comment above it from `"Should have all 48 keywords + 12 types + 9 snippets = 69 minimum"` to `"Should have all 49 keywords + 12 types + 11 snippets = 72 minimum"`, and update the numeric bound from `>= 69` to `>= 72`.

After editing, run the tests to confirm nothing is broken:
`cargo test --manifest-path /Users/sn0w/Documents/dev/snow/compiler/mesh-lsp/Cargo.toml 2>&1`
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/snow && cargo test --manifest-path compiler/mesh-lsp/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `"json"` appears in the KEYWORDS array
    - Comment says "All 49 Mesh keywords"
    - Snippets for `type` and `json` are in the SNIPPETS array (11 total)
    - All 43 LSP tests pass
  </done>
</task>

</tasks>

<verification>
1. Grammar JSON is valid: `python3 -c "import json; json.load(open('tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json'))"`
2. `json` in declaration keywords: `grep -c '"json"' tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json` returns >= 1
3. `nil` in constants: `grep "nil" tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json` shows match in constant.language pattern
4. Atoms pattern exists: `grep "atoms" tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json` shows entries
5. Regex literal pattern exists: `grep "regex-literal" tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json` shows entries
6. LSP tests pass: `cargo test --manifest-path compiler/mesh-lsp/Cargo.toml`
</verification>

<success_criteria>
- TextMate grammar highlights `json`, `nil`, `:atoms`, `~r//`, and `|N>` correctly
- LSP completion list contains all 49 Mesh keywords including `json`
- 2 new snippets (`type` and `json`) available in LSP completions
- All existing LSP tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/133-ensure-the-vscode-extension-is-updated-with-changes-from-milestones-10-11-12-13/133-01-SUMMARY.md`
</output>
