---
phase: 133-ensure-the-vscode-extension-is-updated-with-changes-from-milestones-10-11-12-13
plan: 02
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - tools/editors/vscode-mesh/package.json
  - tools/editors/vscode-mesh/CHANGELOG.md
  - tools/editors/vscode-mesh/mesh-lang-0.3.0.vsix
autonomous: true
requirements: []

must_haves:
  truths:
    - "Extension version is 0.3.0 in package.json"
    - "CHANGELOG.md has a [0.3.0] entry documenting all m10-m13 additions"
    - "A mesh-lang-0.3.0.vsix file exists and was built successfully"
    - "The install-local script in package.json references 0.3.0"
  artifacts:
    - path: "tools/editors/vscode-mesh/package.json"
      provides: "Extension manifest at version 0.3.0"
      contains: "0.3.0"
    - path: "tools/editors/vscode-mesh/CHANGELOG.md"
      provides: "Changelog with 0.3.0 entry"
      contains: "[0.3.0]"
    - path: "tools/editors/vscode-mesh/mesh-lang-0.3.0.vsix"
      provides: "Packaged extension ready for install or marketplace upload"
  key_links:
    - from: "tools/editors/vscode-mesh/package.json"
      to: "tools/editors/vscode-mesh/mesh-lang-0.3.0.vsix"
      via: "npm run package script producing versioned file"
      pattern: "0.3.0"
---

<objective>
Bump the extension version to 0.3.0, document all changes since v0.2.0 in CHANGELOG.md, and produce a new .vsix package.

Purpose: The extension grammar and LSP are updated (plan 01). Now the version number, changelog, and distributable .vsix need to reflect the release.

Output: package.json at 0.3.0, CHANGELOG.md with 0.3.0 entry, mesh-lang-0.3.0.vsix built.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/133-ensure-the-vscode-extension-is-updated-with-changes-from-milestones-10-11-12-13/133-01-SUMMARY.md

<interfaces>
<!-- Current package.json fields relevant to this task: -->
{
  "version": "0.2.0",
  "scripts": {
    "package": "vsce package --no-dependencies",
    "install-local": "vsce package --no-dependencies && code --install-extension mesh-lang-0.2.0.vsix"
  }
}

<!-- Current CHANGELOG.md last entry: -->
## [0.2.0] - 2026-02-14
### Added
- Published to VS Code Marketplace and Open VSX
- Extension icon and marketplace metadata
- Completion suggestions with snippet support
- Signature help for function calls
- Document symbols (Outline view)
### Changed
- Enhanced TextMate grammar with comprehensive scope coverage

<!-- Tool availability: -->
<!-- vsce is available via: cd tools/editors/vscode-mesh && npx vsce package --no-dependencies -->
<!-- TypeScript must be compiled first: cd tools/editors/vscode-mesh && npm run compile -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version and update CHANGELOG</name>
  <files>tools/editors/vscode-mesh/package.json, tools/editors/vscode-mesh/CHANGELOG.md</files>
  <action>
**package.json** — make two changes:
1. Change `"version": "0.2.0"` to `"version": "0.3.0"`
2. Change the install-local script from `"vsce package --no-dependencies && code --install-extension mesh-lang-0.2.0.vsix"` to `"vsce package --no-dependencies && code --install-extension mesh-lang-0.3.0.vsix"`

**CHANGELOG.md** — add a new `[0.3.0]` section at the TOP of the file (above the existing `[0.2.0]` section):

```markdown
## [0.3.0] - 2026-02-28

### Added

- `json` keyword highlighting for JSON object literal syntax (`json { key: value }`)
- Atom literal highlighting (`:name`, `:asc`, `:email`) as constants
- Regex literal highlighting (`~r/pattern/flags`) as strings
- `json` keyword in LSP completion suggestions
- Snippets for `type` alias (`type Alias = ExistingType`) and `json {}` block

### Changed

- Slot pipe operator (`|2>`, `|3>`) now highlights as a pipe operator
- `nil` now highlights as a constant value alongside `true` and `false`
```

Use today's date (2026-02-28) for the changelog entry.
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/snow && python3 -c "import json; pkg=json.load(open('tools/editors/vscode-mesh/package.json')); assert pkg['version'] == '0.3.0', f'version is {pkg[\"version\"]}'; assert '0.3.0' in pkg['scripts']['install-local'], 'install-local not updated'; print('package.json OK')" && grep -q "\[0.3.0\]" tools/editors/vscode-mesh/CHANGELOG.md && echo "CHANGELOG OK"</automated>
  </verify>
  <done>
    - package.json version field is "0.3.0"
    - install-local script references mesh-lang-0.3.0.vsix
    - CHANGELOG.md has a [0.3.0] section at the top with entries for: json keyword, atom literals, regex literals, slot pipe, nil constant, new snippets
  </done>
</task>

<task type="auto">
  <name>Task 2: Compile TypeScript and package VSIX</name>
  <files>tools/editors/vscode-mesh/mesh-lang-0.3.0.vsix</files>
  <action>
Run the following commands from the repo root (use absolute paths):

1. Compile TypeScript:
   `cd /Users/sn0w/Documents/dev/snow/tools/editors/vscode-mesh && npm run compile`

2. Package the extension:
   `cd /Users/sn0w/Documents/dev/snow/tools/editors/vscode-mesh && npm run package`

   This runs `vsce package --no-dependencies` and produces `mesh-lang-0.3.0.vsix`.

If `npm run compile` fails with TypeScript errors, fix the errors before proceeding. The extension.ts source has not changed in this phase so no TS errors are expected.

If the old `mesh-lang-0.2.0.vsix` is still present after packaging the new one, that is fine — do NOT delete it (git tracks it).

After packaging, confirm the .vsix exists:
`ls -la /Users/sn0w/Documents/dev/snow/tools/editors/vscode-mesh/mesh-lang-0.3.0.vsix`
  </action>
  <verify>
    <automated>ls -la /Users/sn0w/Documents/dev/snow/tools/editors/vscode-mesh/mesh-lang-0.3.0.vsix</automated>
  </verify>
  <done>
    - `mesh-lang-0.3.0.vsix` exists in `tools/editors/vscode-mesh/`
    - File size > 0 bytes
    - TypeScript compiled without errors
  </done>
</task>

</tasks>

<verification>
1. Version check: `python3 -c "import json; print(json.load(open('tools/editors/vscode-mesh/package.json'))['version'])"` prints `0.3.0`
2. Changelog: `grep "\[0.3.0\]" tools/editors/vscode-mesh/CHANGELOG.md` returns a match
3. VSIX exists: `ls tools/editors/vscode-mesh/mesh-lang-0.3.0.vsix`
4. Grammar valid: `python3 -c "import json; json.load(open('tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json'))"` exits 0
</verification>

<success_criteria>
- Extension version is 0.3.0 in package.json
- CHANGELOG documents all m10-m13 additions (json, atoms, regex, slot pipe, nil, snippets)
- mesh-lang-0.3.0.vsix built successfully and ready for local install or marketplace upload
</success_criteria>

<output>
After completion, create `.planning/phases/133-ensure-the-vscode-extension-is-updated-with-changes-from-milestones-10-11-12-13/133-02-SUMMARY.md`
</output>
