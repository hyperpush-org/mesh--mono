---
phase: 147-publish-and-verify
plan: 02
type: execute
wave: 2
depends_on: [147-01]
files_modified:
  - mesh-slug/mesh.toml
autonomous: false
requirements: [DIST-01, DIST-02, DIST-03]

user_setup:
  - service: mesh-registry
    why: "Publishing mesh-slug requires a valid registry token tied to a GitHub account"
    env_vars: []
    dashboard_config:
      - task: "Log in to the Mesh package registry via GitHub OAuth"
        location: "https://api.packages.meshlang.dev/auth/github — visit in browser, authorize with GitHub"
      - task: "Create a publish token"
        location: "https://api.packages.meshlang.dev/dashboard/tokens — POST to create, copy the raw token (shown once)"

must_haves:
  truths:
    - "mesh-slug/mesh.toml has name = '{github_login}/mesh-slug' scoped to the publisher's GitHub login"
    - "meshpkg publish succeeds with HTTP 201 (no error output)"
    - "GET https://registry.meshlang.dev/api/v1/packages returns JSON array containing mesh-slug entry"
    - "packages.meshlang.dev shows mesh-slug in search results for query 'slug'"
  artifacts:
    - path: "mesh-slug/mesh.toml"
      provides: "Updated package manifest with scoped name"
      contains: "name = \""
  key_links:
    - from: "meshpkg publish"
      to: "https://registry.meshlang.dev/api/v1/packages"
      via: "POST with Bearer token"
      pattern: "201 Created"
    - from: "packages.meshlang.dev"
      to: "https://registry.meshlang.dev/api/v1/packages?search=slug"
      via: "SvelteKit frontend search"
      pattern: "slug"
---

<objective>
Authenticate with the Mesh package registry, update mesh-slug's name to match the publisher's GitHub namespace, publish the package, and verify it appears on the registry and website.

Purpose: This is the core of Phase 147 — getting mesh-slug live on the registry so any project can install it.

Output: mesh-slug published at `{github_login}/mesh-slug@1.0.0` on registry.meshlang.dev, visible via search on packages.meshlang.dev.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/147-publish-and-verify/147-01-SUMMARY.md

Key facts:
- Registry URL: https://registry.meshlang.dev (default in meshpkg CLI)
- Registry enforces namespace: package name must be {github_login}/mesh-slug (see publish.rs line 30: `name.starts_with(&format!("{}/", owner))`)
- Token obtained via GitHub OAuth: visit https://api.packages.meshlang.dev/auth/github
- Token management dashboard: https://api.packages.meshlang.dev/dashboard/tokens
- meshpkg login stores token at ~/.mesh/credentials as TOML: [registry] token = "..."
- meshpkg publish reads mesh.toml from the project dir and POSTs tarball to registry
- Current mesh-slug/mesh.toml: name = "mesh-slug" — MUST be updated to "{github_login}/mesh-slug"
- Search endpoint: GET https://registry.meshlang.dev/api/v1/packages?search=slug
- packages.meshlang.dev SvelteKit frontend shows search results

<interfaces>
<!-- From mesh-slug/mesh.toml — current content -->
[package]
name = "mesh-slug"
version = "1.0.0"
description = "URL-safe slug generation for Mesh programs"
license = "MIT"

[dependencies]

<!-- meshpkg CLI commands -->
meshpkg login --token <TOKEN>          # stores to ~/.mesh/credentials
meshpkg publish <PROJECT_DIR>          # defaults to current dir if omitted
meshpkg publish --registry <URL>       # override registry URL
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Obtain registry publish token via GitHub OAuth</name>
  <action>Human must obtain a real publish token from the Mesh registry via GitHub OAuth and save it with `meshpkg login --token`. Claude cannot perform browser-based OAuth flows.</action>
  <what-built>Plan 01 fixed the tarball packing and compiler package discovery. Before publishing, we need a valid registry token scoped to the publisher's GitHub account.</what-built>
  <how-to-verify>
    1. Open https://api.packages.meshlang.dev/auth/github in your browser
    2. Complete the GitHub OAuth flow (authorize the Mesh Registry app)
    3. After redirect, visit https://api.packages.meshlang.dev/dashboard/tokens
    4. Create a new token (click the create button) — copy the raw token (it is shown ONCE)
    5. Note your GitHub username (e.g., "snowdamiz") — the package will be published as {username}/mesh-slug
    6. Run in terminal: `meshpkg login --token <YOUR_TOKEN>`
    7. Confirm: `cat ~/.mesh/credentials` shows `token = "your-actual-token"`
  </how-to-verify>
  <resume-signal>Provide your GitHub login username (e.g. "snowdamiz") and confirm token is saved. Type: "ready username={your_login}"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update mesh.toml name and publish mesh-slug to registry</name>
  <files>mesh-slug/mesh.toml</files>
  <action>
After the human provides their GitHub login (from the checkpoint above), perform:

1. **Update mesh-slug/mesh.toml** — change `name = "mesh-slug"` to `name = "{github_login}/mesh-slug"` where `{github_login}` is the GitHub username provided in the checkpoint resume signal (e.g., "snowdamiz" → `name = "snowdamiz/mesh-slug"`).

   The full mesh.toml after update:
   ```toml
   [package]
   name = "snowdamiz/mesh-slug"
   version = "1.0.0"
   description = "URL-safe slug generation for Mesh programs"
   license = "MIT"

   [dependencies]
   ```

2. **Verify the token is saved**: `cat ~/.mesh/credentials` — must show a real token (not "test-token-verify-123").

3. **Run meshpkg publish** from the repo root:
   ```
   ./target/debug/meshpkg publish mesh-slug
   ```
   (Use the debug binary from `cargo build -p meshpkg` done in Plan 01.)

   Expected output:
   ```
   ✓ Published snowdamiz/mesh-slug@1.0.0
     SHA-256: <hash>
   ```

   If you get "unauthorized": the token is stale — re-run the human-action checkpoint.
   If you get "403 Forbidden: Package name must be scoped": the mesh.toml name does not match the GitHub login — verify and fix the name.
   If you get "409 Conflict": the package was already published — this is fine, move to Task 3.

4. **Verify the package appears in the registry**:
   ```
   curl -s https://registry.meshlang.dev/api/v1/packages | python3 -m json.tool
   ```
   Must return a JSON array containing an entry with `"name": "snowdamiz/mesh-slug"` (or your actual login).

5. **Verify package metadata endpoint**:
   ```
   curl -s https://registry.meshlang.dev/api/v1/packages/{github_login}/mesh-slug | python3 -m json.tool
   ```
   Must return JSON with `"latest": {"version": "1.0.0", ...}`.

Commit the mesh.toml change: `git add mesh-slug/mesh.toml && git commit -m "feat(147-02): scope mesh-slug name to registry namespace"`
  </action>
  <verify>
    <automated>curl -sf https://registry.meshlang.dev/api/v1/packages | python3 -c "import sys,json; pkgs=json.load(sys.stdin); print('FOUND' if any('mesh-slug' in p.get('name','') for p in pkgs) else 'NOT FOUND')"</automated>
  </verify>
  <done>mesh-slug/mesh.toml has scoped name. meshpkg publish exits 0. Registry API returns the package in its package list. The package metadata endpoint returns version 1.0.0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Verify mesh-slug appears on packages.meshlang.dev</name>
  <action>Human verifies that the package listing at packages.meshlang.dev shows mesh-slug when searching for "slug". This requires visual browser verification that Claude cannot perform.</action>
  <what-built>mesh-slug has been published to the Mesh registry. The website should now show it in search results.</what-built>
  <how-to-verify>
    1. Open https://packages.meshlang.dev in your browser
    2. In the search box, type "slug" and press Enter (or click search)
    3. Verify: the mesh-slug package appears in the search results with its name and description
    4. Click on the package listing if available to verify the detail page loads
    5. Also verify directly via API: run `curl -s "https://registry.meshlang.dev/api/v1/packages?search=slug" | python3 -m json.tool` — must return non-empty array
  </how-to-verify>
  <resume-signal>Type "approved" if mesh-slug appears in search results, or describe what you see if it does not.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cat mesh-slug/mesh.toml` shows `name = "{github_login}/mesh-slug"`
2. `curl -s https://registry.meshlang.dev/api/v1/packages` includes mesh-slug entry
3. `curl -s "https://registry.meshlang.dev/api/v1/packages?search=slug"` returns non-empty array
4. packages.meshlang.dev search shows mesh-slug (human verified)
</verification>

<success_criteria>
- mesh-slug/mesh.toml has valid scoped name matching publisher's GitHub login (DIST-01)
- meshpkg publish succeeds without error (DIST-02)
- Registry API returns mesh-slug in package list and search results (DIST-03)
- packages.meshlang.dev shows mesh-slug in "slug" search (DIST-03)
</success_criteria>

<output>
After completion, create `.planning/phases/147-publish-and-verify/147-02-SUMMARY.md`
</output>
