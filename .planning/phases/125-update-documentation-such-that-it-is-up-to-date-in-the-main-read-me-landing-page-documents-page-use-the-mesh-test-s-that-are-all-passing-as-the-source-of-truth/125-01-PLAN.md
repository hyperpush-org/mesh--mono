---
phase: 125-update-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "README.md version badge shows v12.0"
    - "README.md performance table shows isolated benchmark results from STATE.md"
    - "README.md project status section reflects current v12.0 stable state"
    - "README.md code examples use valid Mesh syntax that matches passing tests"
    - "README.md install path references compiler/meshc (correct after repo reorg)"
  artifacts:
    - path: "README.md"
      provides: "Accurate top-level project README"
      contains: "v12.0"
  key_links:
    - from: "README.md performance table"
      to: "benchmarks/RESULTS.md"
      via: "link at bottom of table"
      pattern: "Full results and methodology"
---

<objective>
Update README.md to be accurate for v12.0, using isolated benchmark numbers from STATE.md and syntax verified by passing e2e tests.

Purpose: The README is the first thing developers and open source contributors see. It currently shows v6.0 badges, stale co-located benchmark numbers, an outdated project status section, and a web server code example with wrong module casing.

Output: Updated README.md with correct version, performance data, feature list, and code examples.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key facts verified from passing e2e tests and STATE.md. -->

Isolated benchmark results (from STATE.md, runs 2-5 averaged, single server per machine):
  /text endpoint:
    Mesh:   29,108 req/s  p50=2.77ms  p99=16.94ms
    Go:     30,306 req/s  p50=2.95ms  p99=8.51ms
    Rust:   46,244 req/s  p50=2.06ms  p99=4.55ms
    Elixir: 12,441 req/s  p50=6.74ms  p99=25.14ms
  /json endpoint:
    Mesh:   28,955 req/s  p50=2.84ms  p99=16.19ms
    Go:     29,934 req/s  p50=2.97ms  p99=8.40ms
    Rust:   46,234 req/s  p50=2.08ms  p99=4.77ms
    Elixir: 12,733 req/s  p50=7.15ms  p99=23.41ms

Hardware: Fly.io performance-2x VMs (2 vCPU, 4 GB RAM), single server per machine (isolated run),
100 concurrent connections, 30s timed runs × 4 (run 1 excluded, runs 2–5 averaged).

Working HTTP syntax (from tests/e2e/stdlib_http_server_runtime.mpl):
  HTTP.router(), HTTP.route(r, path, handler), HTTP.serve(r, port), HTTP.response(code, body)

String interpolation: both ${} and #{} work. #{} is v12.0 preferred.
Slot pipe: x |2> func(a) — verified by tests/e2e/slot_pipe_basic.mpl
Repo structure after Phase 122: compiler/ mesher/ website/ tools/ at top level.
Install path: cargo install --path compiler/meshc (correct post-reorg).

Current version: v12.0 (Language Ergonomics & Open Source Readiness)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md version, performance table, status, and code examples</name>
  <files>README.md</files>
  <action>
Read the current README.md, then make the following targeted updates:

1. **Version badge** (line 5): Change `version-v6.0-blue` to `version-v12.0-blue`.

2. **Performance section** — Replace the entire performance section body (table + footnote) with isolated benchmark numbers. Use this exact table:

```markdown
Measured on dedicated Fly.io `performance-2x` VMs (2 vCPU, 4 GB RAM), each server running alone (isolated), load generator in the same region over Fly.io's private WireGuard network. 100 concurrent connections, 30 s timed runs × 4 (run 1 excluded, runs 2–5 averaged).

| Language | /text req/s | /json req/s | /text p99 | /json p99 |
|----------|------------|------------|-----------|-----------|
| **Mesh** | **29,108** | **28,955** | 16.94 ms  | 16.19 ms  |
| Go       | 30,306     | 29,934     | 8.51 ms   | 8.40 ms   |
| Rust     | 46,244     | 46,234     | 4.55 ms   | 4.77 ms   |
| Elixir   | 12,441     | 12,733     | 25.14 ms  | 23.41 ms  |
```

Remove the old `†` footnote about cold-start entirely.

3. **Project Status section**: Update to:
```markdown
## Project Status

Mesh is currently in active development.

- **Current Stable:** v12.0 (Language Ergonomics & Open Source Readiness)
- **Recent additions:** Slot pipe operator (`|2>`), `#{}` string interpolation, heredocs, regex literals, environment variable stdlib, and performance benchmarks vs Go, Rust, and Elixir.

See [ROADMAP.md](.planning/ROADMAP.md) for detailed planning and architectural decisions.
```

4. **Web server example** — Replace the web server code block (the one under "### 3. A Web Server Example") with correct HTTP module syntax (uppercase HTTP, using `HTTP.route` not `Http.get`/`Http.post`):

```elixir
module Server

struct User do
  id: Int
  name: String
  email: String
end

fn home_handler(request) do
  HTTP.response(200, "Welcome to Mesh!")
end

fn main() do
  let r = HTTP.router()
  let r = HTTP.route(r, "/", home_handler)
  HTTP.serve(r, 8080)
end
```

5. **Hello World example** — The existing hello world example shows `${count}` inside `HTTP.response`. Update the string interpolation in the hello world example to use `#{}` syntax to showcase v12.0 features. Specifically, in the hero code that shows `HTTP.response(200, "Count: \${count}")`, change `\${count}` to `\#{count}` if it appears in the README (check the raw text — it may use `${}`).

6. **Key Features section** — Add a bullet under "Production Ready" for the new v12.0 features:
   - After the existing bullets, add: `- **String ergonomics:** `#{}` string interpolation, multiline heredocs, regex literals, and `Env.get`/`Env.get_int` for environment variables.`
   - Add: `- **Slot pipe operator:** Route piped values to any argument position with `|N>` syntax.`

7. **Remove** the reference to PROJECT.md in the "Project Status" link (since that's an internal planning file, not ideal for a public README). Keep only the ROADMAP.md link.
  </action>
  <verify>
    <automated>grep -n "v12.0" /Users/sn0w/Documents/dev/snow/README.md && grep -n "29,108" /Users/sn0w/Documents/dev/snow/README.md && grep -n "HTTP.router" /Users/sn0w/Documents/dev/snow/README.md</automated>
  </verify>
  <done>README.md contains "v12.0" in the badge, "29,108" in the performance table, "HTTP.router" in the web server example, and the project status shows v12.0 as current stable.</done>
</task>

</tasks>

<verification>
After completing the task:
1. `grep "v12.0" README.md` — should match the badge and status section
2. `grep "29,108" README.md` — should match the performance table
3. `grep "HTTP.router" README.md` — should match the web server example
4. `grep "version-v6.0" README.md` — should return nothing (old badge gone)
5. `grep "v6.0\|v7.0" README.md` — should return nothing in status sections
</verification>

<success_criteria>
- README.md version badge shows v12.0
- Performance table shows isolated benchmark numbers (Mesh: 29,108 /text, 28,955 /json)
- Web server example uses correct HTTP module syntax (HTTP.router, HTTP.route, HTTP.serve)
- Project status section reflects v12.0 as current stable
- Key Features section mentions slot pipe and string interpolation features
</success_criteria>

<output>
After completion, create `.planning/phases/125-update-docs/125-01-SUMMARY.md` with:
- What was changed in README.md
- Any issues encountered
- Final state of the file
</output>
