---
phase: 123-performance-benchmarks
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-02-26T08:47:07.459Z
---

<current_state>
Mid-execution of Phase 123 Plan 03. Two Fly.io machines are being built via remote builder on `fly-builder-withered-cherry-7010` in the `bench-mesh` Fly.io app (region: ord).

- Server VM (`bench-servers`): Build was in progress — apt-get install complete, next step is Go/Rust install and Mesh compiler build from source. This is a background bash task (ID: bf9071e), output at `/private/tmp/claude-501/-Users-sn0w-Documents-dev-snow/tasks/bf9071e.output`
- Load gen VM (`bench-loadgen`): Build FAILED — `wrk` needs `unzip` to build LuaJIT but it was missing from the apt-get install list. The fix is to add `unzip` to Dockerfile.loadgen then re-run `fly machine run`.

Plans 123-01 and 123-02 are COMPLETE (all benchmark servers created). Plan 123-03 is mid-execution at Task 4 (the human-action checkpoint, but we're doing it automatically via Fly.io CLI).
</current_state>

<completed_work>

**Wave 1 (complete):**
- Plan 123-01: Mesh benchmark server (`benchmarks/mesh/bench.mpl`, port 3000) + Go benchmark server (`benchmarks/go/main.go`, port 3001) — committed
- Plan 123-02: Rust axum server (`benchmarks/rust/Cargo.toml` + `main.rs`, port 3002) + Elixir Plug/Cowboy server (`benchmarks/elixir/mix.exs` + `lib/bench.ex`, port 3003) — committed

**Wave 2 (Plan 123-03, Tasks 1-3 complete):**
- Task 1: `benchmarks/fly/Dockerfile.servers` + `benchmarks/fly/start-servers.sh` — committed (73881737)
- Task 2: `benchmarks/fly/Dockerfile.loadgen` + `benchmarks/fly/run-benchmarks.sh` — committed (4fbdc589)
- Task 3: `benchmarks/fly/README.md` — committed (fe37ef95)

**Fly.io setup:**
- App `bench-mesh` created (personal org)
- `.dockerignore` created at repo root
- `fly machine run` kicked off for both VMs with remote builder (no local Docker needed)
</completed_work>

<remaining_work>

**Immediate: Fix and re-run load gen VM build**
1. Fix `benchmarks/fly/Dockerfile.loadgen` — add `unzip` to apt-get install (needed for wrk's LuaJIT build)
2. Re-run: `fly machine run . -a bench-mesh --name bench-loadgen --dockerfile benchmarks/fly/Dockerfile.loadgen --vm-size performance-2x --region ord --restart no --env SERVER_HOST=bench-servers.vm.bench-mesh.internal --detach`

**Then:**
3. Wait for server VM build to complete (it was compiling apt packages, next: Go install, Rust install, meshc build from Cargo workspace — takes ~15-20 min total)
4. Monitor both machines: `fly logs -a bench-mesh --machine <id>`
5. Check server logs for "=== All servers running ==="
6. Stream load gen logs for benchmark results
7. Collect RSS data from server logs: `fly logs -a bench-mesh --machine <server-id> | grep '^RSS,'`
8. Get runtime versions via SSH
9. **Task 5**: Populate RESULTS.md, METHODOLOGY.md, chart (benchmarks/throughput.png), README.md Performance section
10. Create 123-03-SUMMARY.md
11. Run verifier, update roadmap, complete phase
12. Destroy Fly.io machines: `fly machine destroy --yes -a bench-mesh`

**Also remaining (post-results):**
- Phase verification (gsd-verifier)
- ROADMAP.md update (phase complete)
- Potential gap closure if verifier finds issues
</remaining_work>

<decisions_made>

- Benchmarks run on TWO dedicated Fly.io performance-2x VMs (not localhost) — server VM and load gen VM separate, same region (ord), Fly.io private WireGuard network
- Plan 03 revised mid-execution to use Fly.io two-VM topology (originally was localhost)
- Mesh compiler built from source in Dockerfile using root Cargo workspace (`cargo build --release -p meshc`) — the `mesher/mesher` binary is macOS ARM64, unusable on Linux
- SERVER_HOST uses Fly.io internal DNS: `bench-servers.vm.bench-mesh.internal` (no need to look up private IP)
- Chart generation: Python matplotlib at executor's discretion
- Results methodology: human-action checkpoint replaced by automated Fly.io CLI execution
</decisions_made>

<blockers>

- **Load gen Dockerfile.loadgen needs `unzip`**: wrk's Makefile builds LuaJIT which requires `unzip`. Fix: add `unzip` to `apt-get install -y` in Dockerfile.loadgen. Then re-run fly machine run for bench-loadgen.
- **Server VM build may still be running**: Check task bf9071e. If it completed (success or fail), check `fly machine list -a bench-mesh` for status.
</blockers>

<context>
Phase 123 is the final phase of milestone v12.0 (Language Ergonomics & Open Source Readiness). It creates HTTP performance benchmarks comparing Mesh vs Go/Rust/Elixir. The benchmark infrastructure (4 language servers) is fully built and committed. We're at the "run the actual benchmarks and collect results" step. The Fly.io approach gives reproducible results on dedicated hardware.

The Dockerfile.servers builds the full Mesh compiler from the Rust workspace (compiler/meshc crate). This takes ~15-20 minutes on the remote builder. The load gen image is fast (~3 min) once the unzip fix is applied.

Once both machines are running, the sequence is: server starts 4 HTTP servers → load gen runs wrk against them → results printed to stdout → we capture via `fly logs` → populate RESULTS.md → commit → verify phase.
</context>

<next_action>
Start with: Fix Dockerfile.loadgen (add `unzip` to apt-get install), then check if server VM build completed (`fly machine list -a bench-mesh` or check task bf9071e output). Re-run load gen machine. Then monitor both.

```bash
# 1. Fix Dockerfile.loadgen — add unzip
# Edit line: curl git build-essential libssl-dev bc \
# To:        curl git build-essential libssl-dev bc unzip \

# 2. Check server VM status
fly machine list -a bench-mesh

# 3. Re-run load gen (after fix committed)
fly machine run . -a bench-mesh --name bench-loadgen \
  --dockerfile benchmarks/fly/Dockerfile.loadgen \
  --vm-size performance-2x --region ord \
  --restart no \
  --env SERVER_HOST=bench-servers.vm.bench-mesh.internal \
  --detach
```
</next_action>
