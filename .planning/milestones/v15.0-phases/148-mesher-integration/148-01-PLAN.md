---
phase: 148-mesher-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mesher/mesh.toml
  - mesher/mesh.lock
autonomous: true
requirements:
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "mesher/mesh.toml lists snowdamiz/mesh-slug = \"1.0.0\" under [dependencies]"
    - "meshpkg install succeeds in mesher/ directory with no errors"
    - "mesher/.mesh/packages/snowdamiz/mesh-slug@1.0.0/ exists with slug.mpl inside"
  artifacts:
    - path: "mesher/mesh.toml"
      provides: "Package manifest declaring mesh-slug dependency"
      contains: "snowdamiz/mesh-slug"
    - path: "mesher/mesh.lock"
      provides: "Lockfile pinning mesh-slug version and checksum"
      contains: "snowdamiz/mesh-slug"
  key_links:
    - from: "mesher/mesh.toml"
      to: "mesher/.mesh/packages/snowdamiz/mesh-slug@1.0.0/"
      via: "meshpkg install reads mesh.toml [dependencies] and downloads from api.packages.meshlang.dev"
      pattern: "snowdamiz/mesh-slug.*1\\.0\\.0"
---

<objective>
Add mesh-slug as a declared dependency in Mesher and install it from the live registry.

Purpose: Establishes mesher as a real consumer of a published Mesh package, completing the dependency declaration half of the integration (INTG-01 + INTG-02).
Output: Updated mesher/mesh.toml with [dependencies] entry, mesh.lock lockfile, and installed package at mesher/.mesh/packages/snowdamiz/mesh-slug@1.0.0/
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From mesher/mesh.toml (current state):
```toml
[package]
name = "mesher"
version = "1.0.0"
description = "Mesher monitoring platform — production-grade issue tracker written in Mesh"
license = "MIT"

[dependencies]
# No external mesh package dependencies — uses Mesh stdlib only
```

From tests/e2e/consumer/mesh.toml (reference pattern for dependency declaration):
```toml
[dependencies]
"snowdamiz/mesh-slug" = "1.0.0"
```

From tests/e2e/consumer/mesh.lock (reference pattern for lockfile):
```toml
# shows snowdamiz/mesh-slug@1.0.0 with SHA256 checksum
```

Registry: api.packages.meshlang.dev (default meshpkg registry — do NOT use registry.meshlang.dev which is the Vercel frontend and 404s)

Package install layout: .mesh/packages/{owner}/{name}@{version}/ (scoped packages use two-level layout)
Example: .mesh/packages/snowdamiz/mesh-slug@1.0.0/slug.mpl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mesh-slug dependency to mesher/mesh.toml and run meshpkg install</name>
  <files>mesher/mesh.toml</files>
  <action>
    Edit /Users/sn0w/Documents/dev/mesh/mesher/mesh.toml to add the mesh-slug dependency under [dependencies]:

    ```toml
    [package]
    name = "mesher"
    version = "1.0.0"
    description = "Mesher monitoring platform — production-grade issue tracker written in Mesh"
    license = "MIT"

    [dependencies]
    "snowdamiz/mesh-slug" = "1.0.0"
    ```

    Remove the comment `# No external mesh package dependencies — uses Mesh stdlib only` — it is now outdated.

    Then run meshpkg install from the mesher/ directory:
    ```
    cd /Users/sn0w/Documents/dev/mesh/mesher && /Users/sn0w/Documents/dev/mesh/target/debug/meshpkg install
    ```

    This will:
    1. Read mesh.toml [dependencies]
    2. Download snowdamiz/mesh-slug@1.0.0 from api.packages.meshlang.dev
    3. Install to .mesh/packages/snowdamiz/mesh-slug@1.0.0/
    4. Write mesh.lock with SHA256 checksum

    If meshpkg install fails because ~/.mesh/credentials is missing, check whether credentials are stored there. The token was obtained during Phase 147-02. If the install requires authentication, re-run with --registry https://api.packages.meshlang.dev explicitly.

    Note: meshpkg install (reading mesh.toml) is the same flow that worked in tests/e2e/consumer/ during Phase 147-03. The default registry URL is https://api.packages.meshlang.dev.
  </action>
  <verify>
    <automated>ls /Users/sn0w/Documents/dev/mesh/mesher/.mesh/packages/snowdamiz/mesh-slug@1.0.0/slug.mpl && grep 'snowdamiz/mesh-slug' /Users/sn0w/Documents/dev/mesh/mesher/mesh.toml && grep 'snowdamiz/mesh-slug' /Users/sn0w/Documents/dev/mesh/mesher/mesh.lock</automated>
  </verify>
  <done>mesher/mesh.toml has "snowdamiz/mesh-slug" = "1.0.0" in [dependencies]; mesh.lock exists with checksum; .mesh/packages/snowdamiz/mesh-slug@1.0.0/slug.mpl is present on disk</done>
</task>

</tasks>

<verification>
1. `grep 'snowdamiz/mesh-slug' /Users/sn0w/Documents/dev/mesh/mesher/mesh.toml` returns the dependency line
2. `cat /Users/sn0w/Documents/dev/mesh/mesher/mesh.lock` shows snowdamiz/mesh-slug@1.0.0 with a sha256 checksum
3. `ls /Users/sn0w/Documents/dev/mesh/mesher/.mesh/packages/snowdamiz/mesh-slug@1.0.0/slug.mpl` exits 0
</verification>

<success_criteria>
- mesher/mesh.toml [dependencies] section contains `"snowdamiz/mesh-slug" = "1.0.0"`
- mesher/mesh.lock exists and contains the snowdamiz/mesh-slug@1.0.0 checksum entry
- mesher/.mesh/packages/snowdamiz/mesh-slug@1.0.0/slug.mpl exists on disk (install succeeded)
- INTG-01 satisfied: Mesher's mesh.toml lists mesh-slug as a dependency
- INTG-02 satisfied: meshpkg install succeeded in mesher/
</success_criteria>

<output>
After completion, create `.planning/phases/148-mesher-integration/148-01-SUMMARY.md`
</output>
