---
phase: 148-mesher-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 148-01
files_modified:
  - mesher/storage/queries.mpl
autonomous: false
requirements:
  - INTG-03
  - INTG-04

must_haves:
  truths:
    - "mesher/storage/queries.mpl imports slugify from the Slug module"
    - "insert_org auto-generates slug from the org name when no explicit slug is provided"
    - "meshc build mesher/ completes with zero errors"
    - "Mesher HTTP API endpoints return expected responses after the change"
  artifacts:
    - path: "mesher/storage/queries.mpl"
      provides: "Slug-integrated org insertion with auto-generated slug"
      contains: "from Slug import slugify"
    - path: "mesher/mesher"
      provides: "Compiled Mesher binary with mesh-slug linked"
  key_links:
    - from: "mesher/storage/queries.mpl"
      to: ".mesh/packages/snowdamiz/mesh-slug@1.0.0/slug.mpl"
      via: "from Slug import slugify resolved by meshc discovery Phase 1b (scoped package layout)"
      pattern: "from Slug import slugify"
    - from: "insert_org function"
      to: "slugify(name)"
      via: "auto-generate slug when slug param is empty string"
      pattern: "slugify\\(name\\)"
---

<objective>
Integrate mesh-slug into Mesher: import Slug.slugify in queries.mpl and use it for automatic org slug generation when no explicit slug is provided.

Purpose: Proves the full package workflow end-to-end — Mesher compiles against a registry-installed Mesh package and calls its API in a real code path (INTG-03 + INTG-04).
Output: Modified mesher/storage/queries.mpl with Slug import and slug auto-generation; recompiled mesher binary; human verification that HTTP endpoints still work.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-mesher-integration/148-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From mesher/storage/queries.mpl (current insert_org):
```
pub fn insert_org(pool :: PoolHandle, name :: String, slug :: String) -> String!String do
  let fields = %{"name" => name, "slug" => slug}
  let row = Repo.insert(pool, Organization.__table__(), fields)?
  Ok(Map.get(row, "id"))
end
```

From mesher/services/org.mpl (caller):
```
call CreateOrg(name :: String, slug :: String) do |pool|
  let result = insert_org(pool, name, slug)
  (pool, result)
end
```

From mesher/migrations/20260226000000_seed_default_org.mpl (uses hardcoded slugs):
```
Pool.execute(pool, "INSERT INTO organizations ... VALUES ('Default Organization', 'default') ...", [])
Pool.execute(pool, "INSERT INTO projects ... VALUES ($1::uuid, 'Default Project', 'mesh', 'default') ...", [org_id])
```
Note: The seed migration uses raw SQL INSERT (not insert_org), so changing insert_org's slug logic does NOT affect the seed migration.

From mesh-slug/slug.mpl (Slug module API available after install):
```
pub fn slugify(str :: String) -> String do
  slugify_core(str, "-")
end
```

From tests/e2e/consumer/main.mpl (import pattern for installed packages):
```
from Slug import slugify
```

Mesh import rule: use `from Slug import slugify` — the module name is `Slug` (PascalCase of the file basename slug.mpl).

Mesh language constraints (from decisions log):
- Case arm bodies must appear on same line as -> arrow
- Lambda type annotations: fn(p) -> expr end
- Mesh does NOT support mutual recursion between top-level functions
- meshc build command: `meshc build /path/to/project` or `meshc build` from project dir

meshc binary location: /Users/sn0w/Documents/dev/mesh/target/debug/meshc
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import slugify in queries.mpl and use it for auto-slug in insert_org</name>
  <files>mesher/storage/queries.mpl</files>
  <action>
    Edit /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl:

    1. Add the Slug import at the very top of the file, after the header comment block and before any function definitions:
       ```
       from Slug import slugify
       ```
       The file currently has no top-level imports — it only has function definitions. Add the import as the first non-comment line.

    2. Modify `insert_org` to auto-generate a slug from the org name when the caller passes an empty slug:

    Change from:
    ```
    pub fn insert_org(pool :: PoolHandle, name :: String, slug :: String) -> String!String do
      let fields = %{"name" => name, "slug" => slug}
      let row = Repo.insert(pool, Organization.__table__(), fields)?
      Ok(Map.get(row, "id"))
    end
    ```

    To:
    ```
    pub fn insert_org(pool :: PoolHandle, name :: String, slug :: String) -> String!String do
      let actual_slug = if String.length(slug) == 0 do slugify(name) else slug end
      let fields = %{"name" => name, "slug" => actual_slug}
      let row = Repo.insert(pool, Organization.__table__(), fields)?
      Ok(Map.get(row, "id"))
    end
    ```

    This means:
    - If slug is "" (empty string) → auto-generate via `slugify(name)` (e.g. "My Org" → "my-org")
    - If slug is non-empty → use as provided (existing behavior preserved)

    The seed migration is NOT affected — it uses raw SQL INSERT, not insert_org.

    After editing, compile mesher to confirm zero errors:
    ```
    /Users/sn0w/Documents/dev/mesh/target/debug/meshc build /Users/sn0w/Documents/dev/mesh/mesher
    ```

    If compilation fails with "module Slug not found" — verify that meshpkg install was completed in Plan 01 and that .mesh/packages/snowdamiz/mesh-slug@1.0.0/slug.mpl exists. The discovery.rs Phase 1b fix (from Phase 147-03) handles scoped package layout automatically.

    If compilation fails with a type error on `actual_slug` — check that the if/else branches both return String (`slugify` returns String, `slug` is String — types match, this should not be an issue).
  </action>
  <verify>
    <automated>grep 'from Slug import slugify' /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl && grep 'actual_slug' /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl && /Users/sn0w/Documents/dev/mesh/target/debug/meshc build /Users/sn0w/Documents/dev/mesh/mesher 2>&1 | grep -v "^warning" | tail -5</automated>
  </verify>
  <done>queries.mpl has `from Slug import slugify` import and `insert_org` uses `slugify(name)` for auto-slug generation; `meshc build mesher/` exits 0 with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Mesher compiles and integration is complete</name>
  <files>mesher/storage/queries.mpl</files>
  <action>Human verification of Task 1 output — confirm compiled binary exists and integration is correct.</action>
  <verify>Human confirms: import present, slugify(name) logic present, binary compiled, endpoints respond.</verify>
  <done>Human approves the integration — all 4 INTG requirements satisfied, v15.0 milestone complete.</done>
  <what-built>
    Mesher with mesh-slug integrated: queries.mpl imports and uses Slug.slugify for auto org-slug generation; mesher binary compiled against the registry-installed mesh-slug package.
  </what-built>
  <how-to-verify>
    1. Confirm the import is present:
       `grep "from Slug import" /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl`
       Expected: `from Slug import slugify`

    2. Confirm slug auto-generation is present:
       `grep "actual_slug" /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl`
       Expected: `let actual_slug = if String.length(slug) == 0 do slugify(name) else slug end`

    3. Confirm mesher compiled — binary exists:
       `ls -la /Users/sn0w/Documents/dev/mesh/mesher/mesher`
       Expected: binary file (or wherever meshc places output)

    4. Optional smoke test (if PostgreSQL is running):
       Start mesher and make a request to an existing endpoint (e.g., GET /api/v1/projects/default/issues).
       Confirm it returns a valid JSON response (200 or 404, not a crash).

    All 4 v15.0 success criteria (ROADMAP Phase 148) should now be satisfied:
    - mesher/mesh.toml lists mesh-slug (completed in Plan 01)
    - meshpkg install succeeded in mesher/ (completed in Plan 01)
    - Mesher uses Slug.slugify in a real code path (completed in Task 1)
    - Mesher compiles with zero errors (completed in Task 1)
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the integration is correct, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
1. `grep 'from Slug import slugify' /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl` exits 0 with the import line
2. `grep 'actual_slug' /Users/sn0w/Documents/dev/mesh/mesher/storage/queries.mpl` shows the auto-slug logic
3. `meshc build /Users/sn0w/Documents/dev/mesh/mesher` exits 0 with no errors
4. Human verifies compiled binary exists and functionality intact
</verification>

<success_criteria>
- mesher/storage/queries.mpl imports `slugify` from `Slug` module
- `insert_org` uses `slugify(name)` to auto-generate slug when slug param is empty
- `meshc build mesher/` exits 0 (INTG-04)
- Human confirms binary produced and functionality intact (INTG-03, INTG-04)
- All 4 INTG requirements satisfied — v15.0 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/148-mesher-integration/148-02-SUMMARY.md`
</output>
